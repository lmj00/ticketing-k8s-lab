# 사용: 이 파일을 복사해 프로젝트 루트에 `.env` 로 저장한 뒤 값만 수정한다.
# Docker Compose와 Spring이 같은 MYSQL_PASSWORD 를 쓴다.
#
#   cp .env.example .env

MYSQL_URL=jdbc:mysql://localhost:3306/ticketing?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
MYSQL_USER=root
MYSQL_PASSWORD=root
